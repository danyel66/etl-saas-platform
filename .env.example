# PostgreSQL connection
POSTGRES_USER=postgres
POSTGRES_PASSWORD=yourpassword
POSTGRES_DB=etl_saas_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Optional: full DATABASE_URL overrides individual vars above
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/etl_saas_db

# JWT
SECRET_KEY=change-me-to-a-long-random-secret
